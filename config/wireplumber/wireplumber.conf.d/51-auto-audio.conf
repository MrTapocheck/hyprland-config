# USB DAC priority when connected (generic)

monitor.alsa.rules = [
  {
    matches = [
      { node.name = "~alsa_output.usb-.*" }
    ]
    actions = {
      update-props = {
        priority.session = 1500
      }
    }
  }
]
